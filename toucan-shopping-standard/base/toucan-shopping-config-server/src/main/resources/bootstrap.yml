server:
  port: 9090
spring:
  application:
    name: toucan-shopping-config-server #应用名称
  security: #配置认证
    user:
      name: toucan_sp #用户名
      password: toucan_sp_0987 #密码
  cloud:
    config:
      server:
        native:  #本地配置
          search-locations: "classpath:/config,\
                            classpath:/config/apps/shopping/web"

  profiles:
    active: native

#security:
#  basic:
#    enabled: true
#  user:
#    name: blackbird
#    password: 123456