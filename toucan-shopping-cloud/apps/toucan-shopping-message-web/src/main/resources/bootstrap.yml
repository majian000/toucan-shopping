spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true #名称重复直接覆盖


logging:
  config: classpath:logback-spring.xml


#动态配置,根据服务器的IP在启动的时候动态传入
toucan:
  eureka:
      ip: 127.0.0.1
      port: 8081

---
spring:
  profiles: prod
  application:
    name: toucan-shopping-message-web
  cloud:
    config: #指定配置中心服务端
      username: toucan_sp #用户名
      password: toucan_sp_0987 #密码
      uri: http://localhost:9090
      label: master
      profile: prod


---
spring:
  profiles: dev
  application:
    name: toucan-shopping-message-web
  cloud:
    config: #指定配置中心服务端
      username: toucan_sp #用户名
      password: toucan_sp_0987 #密码
      uri: http://localhost:9090
      label: master
      profile: dev