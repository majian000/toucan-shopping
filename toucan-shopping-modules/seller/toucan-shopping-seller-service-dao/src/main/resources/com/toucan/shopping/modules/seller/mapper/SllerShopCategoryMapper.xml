<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toucan.shopping.modules.seller.mapper.SllerShopCategoryMapper">

    <resultMap id="entityMap" type="com.toucan.shopping.modules.seller.entity.SllerShopCategory" >
        <id column="id" jdbcType="BIGINT"  property="id" />
        <result column="user_main_id" jdbcType="BIGINT" property="userMainId" />
        <result column="shop_id" jdbcType="BIGINT" property="shopId" />
        <result column="category_id" jdbcType="BIGINT" property="categoryId" />
        <result column="delete_status" jdbcType="INTEGER" property="deleteStatus" />
        <result column="create_admin_id" jdbcType="VARCHAR" property="createAdminId" />
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate" />
        <result column="update_admin_id" jdbcType="VARCHAR" property="updateAdminId" />
        <result column="update_date" jdbcType="TIMESTAMP" property="updateDate" />
    </resultMap>


    <insert id="insert" parameterType="com.toucan.shopping.modules.seller.entity.SllerShopCategory" >
        insert into t_sller_shop_category(id,user_main_id,shop_id,category_id,create_date,delete_status)
        values(#{id},#{userMainId},#{shopId},#{categoryId},#{createDate},#{deleteStatus})
    </insert>


    <update id="deleteById" parameterType="java.lang.Long">
        update t_sller_shop_category set delete_status=1 where id = #{id}
    </update>



    <update id="update" parameterType="com.toucan.shopping.modules.seller.entity.SllerShopCategory">
        update t_sller_shop_category set  category_id=#{categoryId},update_date=#{updateDate},update_admin_id=#{updateAdminId}
          where id = #{id} and user_main_id=#{userMainId}
    </update>


    <select id="findListByEntity" resultMap="entityMap" parameterType="com.toucan.shopping.modules.seller.entity.SllerShopCategory">
        select * from t_sller_shop_category t
        <where>
            1=1
            <if test="ids!=null ">
                and t.id in
                <foreach item="item" index="index" collection="ids" open="("  close=")" separator=",">
                    #{item}
                </foreach>
            </if>
            <if test="id!=null">
                and t.id = #{id}
            </if>
            <if test="userMainId!=null">
                and t.user_main_id = #{userMainId}
            </if>
            and t.delete_status = 0
        </where>

    </select>

</mapper>